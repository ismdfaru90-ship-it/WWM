{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "containers/app/Dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "startCommand": "NO_SETUP=true SANDBOX_USER_ID=1000 /app/entrypoint.sh uvicorn openhands.app_server.app:app --host 0.0.0.0 --port 3000"
  }
}
